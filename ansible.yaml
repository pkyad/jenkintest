- hosts: webservers
  become: yes
  become_method: sudo
  tasks:
    - name: apply
      command: kubectl apply -f /home/toolsadmin/Desktop/kubernetes-postgres/app/shapp-deploy.yaml
    - name: reload app
      command: kubectl rollout restart deployment.apps/shpl-app
    - name: reload celery 
      command: kubectl rollout restart deployment.apps/celery-worker